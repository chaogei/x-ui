name: CI

on:
  push:
    branches: ['**']
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  GO_VERSION: "1.22"

jobs:
  build-and-test:
    name: build / vet / test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      # go.sum 曾经缺失 ~363 条目而没人发现，因为没有 CI。
      # -mod=readonly 会在 go.mod/go.sum 与源码不一致时直接失败。
      - name: Verify go.mod / go.sum are tidy
        run: |
          set -euo pipefail
          cp go.mod go.mod.bak
          cp go.sum go.sum.bak
          go mod tidy
          diff -u go.mod.bak go.mod
          diff -u go.sum.bak go.sum
          rm go.mod.bak go.sum.bak

      - name: gofmt
        run: |
          set -euo pipefail
          unformatted=$(gofmt -l $(git ls-files '*.go'))
          if [ -n "$unformatted" ]; then
            echo "These files are not gofmt-clean:"
            echo "$unformatted"
            exit 1
          fi

      - name: Build
        run: go build ./...

      # 纯 Go SQLite 驱动意味着 CGO_ENABLED=0 也能构建；这里额外验证一次，
      # 保证 release.yml 的交叉编译（同样 CGO_ENABLED=0）不会在 tag 时才炸。
      - name: Build without cgo
        env:
          CGO_ENABLED: 0
        run: go build ./...

      - name: Vet
        run: go vet ./...

      # 面板的部署形态是 CGO_ENABLED=0 的静态二进制，所以先按那个配置跑一遍
      # 测试：驱动、embed、模板解析都要在真正发布的构建条件下验证过。
      - name: Test
        env:
          CGO_ENABLED: 0
        run: go test -count=1 -timeout 15m ./...

      # -race 需要 cgo，ubuntu-latest 自带 gcc。
      # 端到端用例里有并发登录与并发混合语言渲染，就是冲着这一步来的。
      - name: Test (race)
        env:
          CGO_ENABLED: 1
        run: go test -race -count=1 -timeout 20m ./...
